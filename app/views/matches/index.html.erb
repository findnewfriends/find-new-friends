
<!-- FILTER OPTIONS -->
<div class="container-fluid bg-light pt-4">
<div class="text-center">
  <h2 class="mb-3 text-info">Find your new mate today! </h2>
  <p class="text-secondary">It's as easy as it looks ! Just select the field you are looking for and click search <i class="far fa-smile-beam"></i>.</p>
</div>
	<div class="row align-items-center justify-content-center">
      <div class="col-md-2 pt-3">
        <div class="form-group ">
          <select id="inputState " class="form-control">
            <option selected>Age</option>
            <option>Less than 25</option>
            <option>25-30</option>
            <option>30-35</option>
            <option>35-40</option>
            <option>40 +</option>
          </select>
        </div>
    </div>
    <div class="col-md-2 pt-3">
      <div class="form-group">
        <select id="inputState" class="form-control">
          <option selected>Gender</option>
          <option>Male</option>
          <option>Female</option>
        </select>
      </div>
    </div>
    <div class="col-md-2 pt-3">
      <div class="form-group">
        <select id="inputState" class="form-control">
          <option selected>Activities</option>
          <option>Video games</option>
          <option>Hiking</option>
          <option>Music</option>
          <option>Travel</option>
        </select>
      </div>
    </div>
    <div class="col-md-2 pt-3">
      <div class="form-group">
        <select id="inputState" class="form-control">
          <option selected>Location</option>
          <option>Paris</option>
          <option>Lyon</option>
          <option>Toulouse</option>
          <option>Montpellier</option>
          <option>Bordeaux</option>
        </select>
      </div>
    </div>
    <div class="col-md-2 buttonSearch">
      <button type="button" class="btn btn-info btn-block">Search</button>
    </div>
  </div>
</div>

<!-- ENNND FILTER OPTIONS -->

<!-- Card Profile -->
<div class="cardProfile">
  <div class="container-fluid mt-5 ml-2 mr-2 mb-5 justify-content-center">
   <div class="row justify-content-center">
     <!-- First card Profile -->
   <% @users.each do |user|%>
      <div class="card m-1 col-xl-3 col-sm-12 col-md-8 col-lg-5 shadow backgroundColorCard">
        <%= image_tag("avatar.jpg", class:'mt-4 img_profile') %>
        <div class="card-body">
          <h6 class="m-0 text-white text-center text-uppercase">
          <% if user.first_name && user.last_name%>
            <%= user.first_name  + ' ' + user.last_name%>
          <% else %>
          No name yet
          <% end%>
           </h6>
          <h6 class="m-0 mt-2 text-white text-center"><i class="fas fa-map-marker-alt text-gray-300" id="iconLocation"></i>
           <% if user.city%>
            <%= user.city.name %>
          <%else %>
          No city yet
          <% end%>
           </h6>
          <hr>
          <div class="text-center">
            <h6 class="m-0 mb-2 text-white text-uppercase">Bio</h6>
          </div>
          <p class="text-white text-center">
            <%= user.description %>
          </p>
          <hr>
          <p class="text-dark text-center">
            Score: <%= (1/(match.score+0.2)).round(2) %>
          </p>
          <hr>
          <div class="text-center">
            <%= link_to "Send match request", root_path, class:"btn btn-outline btn-sm text-uppercase mt-4" %><br/>
            <%= link_to "See profile", match_path(user.id), class:"btn btn-outline btn-sm text-uppercase mt-4" %>
          </div>
        </div>
      </div>
    <% end %>

    <!-- End first card profile-->
    </div>
  </div>
</div>
<!-- END Card Profile -->
